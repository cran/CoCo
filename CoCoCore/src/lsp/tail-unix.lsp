
;; (load (concatenate 'string *xlisp-cocolib* "/oldcfun.lsp"))

;; (load (concatenate 'string *xlisp-cocolib* "/cocoshlib.lsp"))

(defun get-lib-and-tmp ()
  (let ((result (concatenate 'string "@" *cocolib* "@" "/tmp" "@")))
    result)
  )
