set output diary  continuous.1.exclude.dia;
set output dump   continuous.1.exclude.dmp;
set output report continuous.1.exclude.rpt;

import data survey-continuous-exclude.xpt;

dispose models all models;
exclude missing in *;

read model hc.; current; base;
add edge gc; current; base; read model g,hc.; find -2log(Q);
read model gc.; current; base;
read model g,c.; find -2log(Q);

read model hc.; current; base;
add edge cY; current; base; read model Y,hc.; find -2log(Q);
read model cY.; current; base;
read model c,Y.; find -2log(Q);

read model hc.; current; base;
add edge gm; current; base; read model g,m,hc.; find -2log(Q);
read model gm.; current; base;
read model g,m.; find -2log(Q);

read model .;
current;
base;
recursive headlong coherent forward;

show tests;

end;
