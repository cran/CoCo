set output diary  tst.000.dia;
set output dump   tst.000.dmp;
set output report tst.000.rpt;

import data survey-continuous.xpt;

dispose models all models;
exclude missing in *;

source options.src;

read model gc.; current; base;
read model g,c.; find -2log(Q);

end;
