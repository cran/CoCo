set output diary  tst.00.dia;
set output dump   tst.00.dmp;
set output report tst.00.rpt;

import data survey-continuous.xpt;

dispose models all models;
exclude missing in *;

source options.src;

read model hc.; current; base;
add edge gc; current; base; 
read model g,hc.; find -2log(Q);

dispose of tables;

read model gc.; current; base;
read model g,c.; find -2log(Q);

end;
