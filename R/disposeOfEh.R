"disposeOfEh" <-
function (code = "all", data = NULL, object = .object.of.thing(data = data, 
    ...), ...) 
{
    coco.simple.command(216, .encode(c("all", "duals", "a.dual",  "a.duals", 
        "r.dual", "r.duals", "classes", "accepted", "rejected"), code, 
        c(10, 1, 2, 2, 3, 3, 5, 6, 7), 0), object = object)
}
